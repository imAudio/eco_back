<form method="post" action="{{ path('app_river_delete', {'id': river.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ river.id) }}">
    <button class="btn">Delete</button>
</form>
