{% extends 'base.html.twig' %}

{% block title %}River{% endblock %}

{% block body %}
    <h1>River</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ river.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_river_index') }}">back to list</a>

    <a href="{{ path('app_river_edit', {'id': river.id}) }}">edit</a>

    {{ include('river/_delete_form.html.twig') }}
{% endblock %}
