{% extends 'base.html.twig' %}

{% block title %}Combo{% endblock %}

{% block body %}
    <h1>Combo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ combo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ combo.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ combo.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_combo_index') }}">back to list</a>

    <a href="{{ path('app_combo_edit', {'id': combo.id}) }}">edit</a>

    {{ include('combo/_delete_form.html.twig') }}
{% endblock %}
